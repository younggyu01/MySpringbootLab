@host = http://localhost:8080

# ISBN
@isbn1 = 9788956746425
@isbn2 = 9788956746432

# 기본값, 실제 호출 성공 시 덮어씌워짐
@book1Id = 1
@book2Id = 2

### 책1: ISBN으로 조회해서 id 세팅 (이미 DB에 있으므로 200)
GET {{host}}/api/books/isbn/{{isbn1}}

> {%
    const body = (typeof response.body === 'string') ? JSON.parse(response.body) : response.body;
    if (body && body.id) client.global.set("book1Id", body.id);
%}

### 책2: ISBN으로 조회해서 id 세팅
GET {{host}}/api/books/isbn/{{isbn2}}

> {%
    const body = (typeof response.body === 'string') ? JSON.parse(response.body) : response.body;
    if (body && body.id) client.global.set("book2Id", body.id);
%}

### 전체 조회
GET {{host}}/api/books

### ID로 조회 (책1)
GET {{host}}/api/books/{{book1Id}}

### 수정 (책2 가격 → 36000)
PUT {{host}}/api/books/{{book2Id}}
Content-Type: application/json

{
  "title": "JPA 프로그래밍",
  "author": "박둘리",
  "isbn": "{{isbn2}}",
  "price": 36000,
  "publishDate": "2025-04-30"
}

### 삭제 (책2)
DELETE {{host}}/api/books/{{book2Id}}

### 삭제 확인(404)
GET {{host}}/api/books/{{book2Id}}
